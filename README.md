# 生长词库 · 完整生命周期演进文档

**目标**：从起点中文网标题 CSV（**8887 本书名**）中，通过 BFS 字生长算法提取高频词组（**7150 个候选词**），
并滤除寄生碎片，保留真正的书名与自由词——用于**统计网站标题取名高频词**。

**核心矛盾**：BFS 生长出来的候选词里，混入了大量"寄生词"——它们不是独立词，只是总出现在某个固定
搭配旁边（如 `之魂` 永远跟在 "X之魂" 后面、`聊天` 永远 92% 紧邻 "群"）。需要一种判据
把这些寄生词筛掉，同时保留真正的书名和自由搭配。

**本文档范围（完整生命周期）**：覆盖从 v1（BFS 生长 + 独立出现次数判据）到 2.1.x（复合熵过滤演进）、
外来方法对照（凝固度 PMI）、三版本生产验收与错题集的**全阶段**。**口径澄清**：`8887 = 书本数`，
`7150 = BFS 生长出的候选词数`（一个书名会拆出多个候选串），两者不是一回事；各版本"保留词数"
（5865~5877）也都是候选词池里的词数，不是书本数。

> **阅读提示（演进逻辑）**：本文档按"**缺憾 → 真数据表现 → 改进 → 新缺憾**"的因果链组织。
> 每个版本之所以存在，都是因为上一个版本在真实数据上暴露了具体缺憾；调参的精髓在于
> **每个参数都是一个跷跷板**——调大放出某类错误、调小误杀某类正确词，没有免费午餐。

---

## 〇、完整生命周期速览（v1 → 错题集）+ git 分支谱系

**这个项目不是从"复合熵"开始的。** 完整生命周期分三段：

| 阶段 | 覆盖版本 | 内容 | 谁是主导 |
|---|---|---|---|
| **生长** | v1 (600c613) | BFS 跳跃枚举最大重复 + **独立出现次数判据**（v1 核心创新） | 用户原创 |
| **展示** | v2.0/v2.1/2.1.1/2.1.2/2.1.3 | 互动词云 → 去耦模块 → 空壳引擎 → 全量展示 → 界面优化 | 用户原创（含"最小改动"铁律） |
| **过滤** | 2.1.4 → 2.1.17 | 复合熵演进（含 3 条失败路线）+ 外来凝固度 | 判据用户原创 / 凝固度外来 |

**git 分支谱系（重命名后；次版本号 = 从主干长出的第几个平行方向，修订号 = 方向内部迭代）**：

```
main (2.1.x 主干连续迭代，停在 2.1.11 熵定稿)   ← 你的最终认可基线
├─ 2.1.12-punct-empty             实验：PUNCT 视为空 → 结构性矛盾，废弃
├─ 2.1.13-matrix                  实验：矩阵方案 → 独立率信号无区分力，废弃
├─ 2.1.14-indep-ratio             实验：独立率保护 → 97%复活，废弃
│  └─ 2.1.15-indep-pre            换顺序 → 数学等价，废弃
├─ 2.2.1-pos-fixed  ★自研最强版  方向①：位置固定度豁免（救真词）
├─ 2.3.1-cohesion                 方向②：外来凝固度 PMI（清碎片）
│  └─ 2.3.2-ami                   方向② 内部迭代：AMI/NPMI 试错，不敌 min-PMI
└─ 2.4.1-spe                      方向③：SPE 超词位置熵（main 直分，第4信号）
```

> 历史 commit 里仍写旧号（2.1.16/2.1.17/2.1.18），重命名不改写历史，这是预期的。

**"活"版本**：main(2.1.11) 单熵 / 2.2.1 位置固定度 / 2.3.1 凝固度（正交互补）——前两个加 2.4.1 SPE 是四版本对比对象。

---

## 一、算法基础（快速回顾）

### 第一层：BFS 生长 + 独立出现次数判据（v1，全项目地基）

BFS 字生长算法以单字为种子，从语料中枚举所有重复出现的字串（count≥2），生长出候选词池（7150 个）。每个候选词产出基础指标：

```
(w, count, independent, binding, compound_entropy)
```

- `count`：出现总次数；`binding`：集中度（`--bind`，默认 1.0 关闭）。
- **`independent`（独立出现次数）= 不被任何更长词覆盖的出现次数**——这是 v1 的核心创新，解决了**分布式嵌套盲区**：`霸道` count=12 但独立=0（=霸道总裁40+霸道千金40 拆出来的），被精准删除；`霸道总裁` 独立=1，保留。真数据验证：`我真` count=98 但独立仅 3（几乎全嵌"我真没想…"模板），被压到该有的位置。
- 判据：`保留 iff independent ≥ 1 且 len ≥ 2`。400 组随机语料对拍（`verify.py`）全过。

> 这一层**不涉及熵**。它处理的是"这个词是否被更长词覆盖"（嵌套寄生）；复合熵处理的是"这个词是否焊死在固定搭配旁"（邻接寄生）——**两层解决的是两类不同问题**。

### 第二层：复合熵（compound_entropy，2.1.4 起）

复合熵衡量"词对外的自由度"——它是不是总挨着固定的字。核心创新点在 `compound_entropy`——**复合熵**。

### 每个词的出现有一个左邻和一个右邻，分为三类：

| 类型 | 例子 | 参与熵计算？ | 算作"有邻居"？ |
|---|---|---|---|
| **汉字** | 实打实的 CJK 字符 | 是，按字计数 | **是**（这是"依附判定"的唯一依据） |
| **符号(PUNCT)** | 标点/数字/字母统统折叠成一个哨兵 `\ue000` | 是，按次数累计 | **否**（符号邻居不表示"有依附对象"） |
| **空(SEP)** | 串首/串尾/SEP 标记 | 否（不存在邻居，不计入） | 否 |

> 三类**互斥且和为总出现次数**。比如 `什么鬼` 出现 23 次，右邻三类分别是：汉字"上"×1、符号(PUNCT)×10、空×12 → 1+10+12=23。

### 复合熵的判定链路

```
某侧"无汉字邻居"占比 ≥ 80%（空+符）？
  → 是 → 该侧是"位置使然"（句首/句末），忽略该侧，只看另一侧

否则进入标准判据：

一侧全空（无汉字、无 PUNCT）→ 该侧忽略，用另一侧算熵
  另一侧数据 < 3 次 → 豁免（信息太少，不能判定依附）

两侧都有汉字：
  少数侧非空 / 多数侧非空 < 25%  → 合并两侧分布，算总熵
  否则                              → min(左熵, 右熵)

最终：熵 < 0.5 → 判为依附词，过滤
```

> **为什么用 min？** 一个词叫"依附"，只要**有一侧**焊死在固定字上就行——`聊天` 右侧 92% 是"群"，
> 即使左侧接字万般自由，它也是"聊天群"的寄生碎片。min 保证了两侧只要一边低即判依附。

---

## 二、版本演进：问题 → 发现 → 改进 → 新问题

### v1 — BFS 生长 + 独立出现次数判据（项目起点，非互动）

**做法**：CSV → 去重加权 → 硬边界清洗（只留 CJK）→ BFS 跳跃枚举最大重复 → 独立出现次数判据 → 词频/字频/词云 PNG。

**真实数据验证（8887 本书，4.3 秒）**：
| 词 | count | independent | 说明 |
|---|---|---|---|
| 重生 | 334 | 120 | 网文第一题材，独立使用率 36% |
| 我真 | 98 | **3** | 几乎全嵌"我真没想…"模板，独立仅 3 |
| 霸道 | 12 | **0** | 分布式嵌套盲区——被精准删除 |
| 从庆余年开始 | 3 | 3 | "从X开始"穿越流模板成最大重复 |

**遗留问题**：`independent` 只挡"被更长词覆盖"的嵌套寄生，挡不住"总焊在固定搭配旁"的邻接寄生——`之魂` 永远跟在 "X之魂" 后面、`聊天` 永远 92% 紧邻 "群"。**这是复合熵出现的直接动因。**

---

### v2.0 · v2.1 — 互动词云 + 去耦（8-11 下午）

**v2.0**（内联版）：用户提出互动词云需求——单击变色、双击弹出可拖动表格面板（列出匹配书名、词高亮）。
**v2.1**（去耦版）：按用户"最小改动、插入模块"铁律，互动逻辑抽入 `interactive_cloud.py`，`grow.py` 仅 +28/−9 行（全是接入性质）。

**两个真 bug 被用户抓出**：① 模型验证"内联=去耦"时用模块自己生成的和自己比（循环论证），被用户一句"你不会干出自己和自己对比的傻事吧"点破后独立重验，才暴露 wordcloud 布局非确定（未固定随机种子）；② 抽取过程中 `esc` 正则丢了 `'` 转义。修完按 2.0/2.1 双版本入 git。

---

### 2.1.1 — 空壳引擎（数据/引擎解耦）★用户架构洞见

**用户提出**：把词频数据从 HTML 抽出来放表格，HTML 成为空壳的词云生成引擎——数据多大都不影响显示性能，窗口固定只截取最高频部分。

**落地**：外壳 HTML 恒定 5.7KB + 外部 `data.js`（可任意大）；`--standalone` 保留内联兜底。用 `<script src>` 而非 `fetch`，绕开 `file://` 下 CORS 拦截，保住"双击直开"。

---

### 2.1.2 — 移除 50 条截断，全量展示

**用户提出**："列表里的小说哪里有分先后和重要与否的？都有拖动条了，还整选择展示的？"——展示层不做价值排序，全量展示 + 滚动条。

---

### 2.1.3 — 界面优化：面板居中 + 右上搜索栏

**用户提出**：① 双击屏幕下方词时拖动框被挤出可视区 → 强制纵向居中屏幕中线；② 右上实时搜索栏，输入关键字匹配词、按包含标题数降序（词 + 计数同列）。

**⚠️ 这是全项目最重要的转折点**：你亲手用 2.1.3 刚加的搜索栏去搜词，搜出三个真实噪音例子——
- `之下`：23 条里 21 条嵌在"一人之下"，脱离就是小众标题；
- `界的`：高度依赖"世界的"，前面总是"世"或"万"；
- `生从`：要么"长生"、要么"重生"，两项集中了大部分权重。

**→ 这三个例子直接催生了 2.1.4 的复合熵。功能推动认知，认知催生算法。**（版本号 2.1.9 不存在：用户伪代码首次实现时 merge/min 逻辑装反，被 2.1.10 定稿取代。）

---

### 2.1.4 — 首次引入复合熵（min 判据）

**做法**：BFS 生长的每个候选词，计算左右邻居的信息熵，取 `min(左熵, 右熵)` 作为依附判据。

**首先暴露的问题**：
标题语料中，词大量独立成段出现在"一行就是一条标题"的模式里。`吞噬星空` 出现 20 次，19 次左邻是
空（在标题开头），1 次左邻是"从"。按照"空不计入熵"的规则，左熵 = H([1]) = 0。右熵正常。
min(0, 1.43) = 0 → **被误杀**。

**根因**："全独立成词"和"焊在固定字上"在 min 判据里**长得一模一样**——都是一侧熵为 0。
`吞噬星空` 的 0 是因为 19/20 是空（独立标题），`剑修` 的 0 是因为永远跟着"我"。
**min 不区分这两者。**

→ 这直接催生了 2.1.6。

---

### 2.1.5 · 2.1.6 — 标点感知 + 合并熵模式

**2.1.5**：引入 PUNCT 哨兵——非 CJK 字符不丢弃，折叠成同一个"特殊汉字"参与熵分布。
意图是让标点提供额外的邻居信号。

**2.1.6**：引入**合并熵**——当一侧邻居数据极少时（少侧/多侧 < 10%），不取 min，而是把
左右邻居揉在一起算总熵。专门针对 `吞噬星空` 这种"只因为独立成词太多导致一侧数据真空"的词。

**合并熵的效果**：

| 词 | 之前(min) | 之后(合并) | 结果 |
|---|---|---|---|
| 吞噬星空 | 0 | 1.64 | ✅ 救回 |
| 一人之下 | 0 | 1.24 | ✅ 救回 |
| 之主 / 之王 | 0 | 4.64 | ✅ 保留（认可） |
| 我能 | 0 | 0.23 | 仍滤（右侧 38 种接字，但左 PUNCT×26 → 不触发合并 → min 滤） |
| 剑修 / 剑客 | 0 | 0 | 仍滤 |

**好在哪里**：从"只看僵化侧"变成"如果数据极度不对称就合并"，救回了大量被 min 误杀的书名。

**2.1.6 留下的问题**：
合并路径只处理了"数据极度不平衡"。但中间情况——双侧数据勉强平衡但一侧低熵——仍然靠 min 杀，
比如 `一人之下` 在合并模式下救回了，但 `什么鬼`（右侧 23 次里仅 1 次汉字"上"、22 次是空/PUNCT）
的右侧不空比例 1/23=4.3% 虽然也应合并，但在 18% 合并比下刚好被挡住。

更重要的是，**2.1.5 的 PUNCT 哨兵引入了一个新 bug**——见 2.1.7。

---

### 2.1.7 — 修正：PUNCT 多算成"真实邻居"

**问题发现**：`长生修仙` 这个纯独立标题（每次出现左右都是"空/符号/串首串尾"），在 2.1.5 中
被踢出豁免、进入统计层 → 熵 = 0 → 被滤。

**根因**：2.1.5 把 PUNCT 算成了"真实邻居"。`长生修仙` 右邻确实有 PUNCT（标题末尾的冒号），
但 PUNCT 邻居 ≠ 有义邻居。**功能上"标点参与了"不代表"标点该被当成依附证据"。**

**修正**：PUNCT 只参与熵分布（提供统计信号），**不作"是否有真实邻居"的判定**。
豁免判定只看纯 CJK：两侧皆无汉字 → 豁免(-1.0)。

**效果**：`长生修仙`/`人在木叶`/`全职法师` 等纯独立标题全部恢复 -1.0 豁免保留。
这次修正把 2.1.5 埋的雷排掉了，但也暴露了 2.1.6"合并熵"的另一个盲区——见 2.1.8。

---

### 2.1.8 — 修正：一侧纯无效就忽略整侧

**问题发现**：`万族`（总在句首出现——左邻 100% 是空）因为左边全是空，按 2.1.6 的逻辑：
"空不计入熵"→ 左邻一整个空 → 左侧被压缩成"单一邻居"→ 左熵 0 → min(0, H右) = 0 → **被滤**。

**用户指出这是 bug**：如果一侧全是无效数据，那根本不能说"这一侧依附"——
那是**没数据**，不是依附证据。

**修正**：一侧 100% 空（无汉字邻居）→ 忽略该侧，只看另一侧。
`万族`：左全空 → 忽略 → 只看右侧（邻居 5 种）→ 熵 2.0 → **保留** ✅

**但引入的新边界问题**：`一人之下` 左邻 21 次中只有 2 次是汉字"从"，19 次是空/PUNCT。
2 次"有汉字" → 不算"全空" → 按正常流程：左仅 1 种汉字 → 左熵 0 → min(0, 1.24) = 0 → **滤**。

**为什么不能同时救它**：2 次汉字正好打碎了"一侧全空→忽略"的保护——它不是全空，它只是**几乎全空**。
而合并路径需要少/多 < 25%：左侧汉字 2 次，右侧汉字 4 次 → 比例 50%，远不触发合并。

→ 这为 2.1.10 的"位置固定度"概念埋下了伏笔。

---

### 2.1.10 — 邻居三态重构 + clean() 修复

**这是用户反复纠正后终于对齐的定稿版本。核心改进：**

**① 邻居三态规则**明确"符号是一个特殊汉字"——逐字符计数（"LPL史记"→ PUNCT×3 + 史记×2），
参与熵分布、不作"有邻居"判定、输出切掉。

**② clean() 修复**：`PUNCT.join(runs)` 会丢弃首尾孤立符号（"LPL史记"→"史记"，LPL 丢失）。
改为 `re.sub(r'[^\u4e00-\u9fff]', PUNCT, t)` 逐字保留。

**③ 合并触发比从 10% 调到 20%**：`苟在` 左不空 3、右不空 23 → 比例 13%，10% 不触发 → 滤。
用户说 10% 太严，放宽到 20% → 苟在救回 ✅。

**④ 单侧数据不足豁免**：只用一侧算熵且该侧不空 < 3 次 → 豁免。`斗破苍穹` 右仅"之"×1、
`史记` 右仅"之"/"是"各 2 次 — **"数据太少回答不了'归属于谁'，就不滤"** （用户原话）。

**⑤ 合并比 20% 虽然修复了苟在，但放掉了什么鬼吗？**
没有。`什么鬼` 右汉字仅"上"×1+右 PUNCT×10 → 右不空=11，左不空=22 → 比例 50% > 20%，不触发合并 → min 滤。
**什么鬼 在此版本仍被误杀**——这成了后续改进的直接驱动力。

---

### 2.1.11 — 参数网格调参定档 → main 接受版

**做法**：在 2.1.10 定稿规则上，用金标准词集（keep 36 / filt 25）做 5×3 参数网格，
对 `--min-ent` 和 `--ent-merge-ratio` 全面扫描。

**金标准（手动标注）**：
- 应留 37 词：吞噬星空、一人之下、长生修仙、万族、斗破苍穹、诛仙、史记、无限恐怖、鬼灭、苟在、之王、世界、长生、凡人、修仙、都市、系统、巅峰、重生之、人在木叶、全职法师……
- 应滤 25 词：我能、剑修、剑客、你管、联盟之、无限之、诸天之、之巅、之魂、人在斗……

#### 发现：min-ent 在 0.5 产生最优切断

语料的熵直方图呈现**天然双峰**：寄生集群在 [0.0, 0.3]（BFS 片段→一侧退化单一邻居→熵≈0），
自由词集群在 [1.0, 1.5]。谷底在 0.5，正适合做切断阈值。

#### 什么参数怎么改，会产生什么错误：

| 参数 | 调到 | 放出的错误（寄生复活） | 误杀的正确词 |
|---|---|---|---|
| `--min-ent` | **降**到 0.3 | 寄生集体复活：filter 从 21/25 掉到 6~8/25——人在斗、影开始、聊天 全跑出来了。因为寄生词一侧退化邻居=0 熵，min-ent=0.3 对他们完全无效 | 几乎无误杀（全放出来了） |
| | **升**到 0.7 | 寄生复活风险清零 | **误伤 65~101 个 count≥5 的真实自由词**：这些词低频→邻据稀疏→熵天然就靠近 0.5~0.7，提高阈值把他们也一起杀了。提升阈值增加过滤量，但边际收益为零（filt 只多 +1 寄生，误伤翻倍） |
| `--ent-merge-ratio` | **降**到 0.10（极少触发合并） | 无 | 漏救大量"一侧几乎空"的自由词（苟在 13% 不触发、完美世界等低不空比词不再合并路径 → 保留引证不足 → min 杀） |
| | **升**到 0.30（激进合并） | **漏滤 2 个寄生**：之巅(熵 2.81)、之魂(熵 2.56) 在合并模式下原本靠 min 杀，但合并路径把两侧揉在一起后熵抬高了→这两其实算"之主/之王"同类酌情保留 | 误伤 32 个真实词（mr=0.25 救回了，mr=0.20 漏救） |

#### 最终定档：--min-ent 0.5 + --ent-merge-ratio 0.25
- keep：36/37 ✅（一人之下 在 0.5 阈值 + 20% 合并比下未救回，但后续版本修复）
- filt：21/25（4 个未滤的寄生之巅/之神/之子/我只 熵 >1.2，提阈值也滤不掉，属"X之Y"结构 用户认可保留）
- 词数：7150→5865（滤 1285 个，18.0%）
- **但 `什么鬼` 仍被误杀**——右不空 11/左不空 22=50%>20% 不触发合并 → min(4.06, 0.44)=0.44 < 0.5 → 滤。

---

### 2.1.12 — 第一次尝试救"什么鬼"，因结构性矛盾放弃

**触发**：用户拿到 top100 排除词的邻居分布（`exp/excluded_top100.md`），发现 `什么鬼`(#4, cnt=23) 
在真实数据上的分布：右邻 23 次中汉字仅"上"×1、PUNCT×10、空×12。
**这个分布不表征依附——它只是因为高频出现在句末。**

**尝试**：PUNCT 在熵计算中全局视为空（不计分布、不参与合并决策）。

**效果是自毁的**：什么鬼 救回了 ✅，但：
- `一人之下` 被误杀 ❌：左汉字仅"从"×2、PUNCT×17 变空后，2/4=50%（比例反而涨了）→ 不触发合并 → min 杀
- `我能` 复活 ❌：左汉字仅"的"×1、PUNCT×26 变空后，1/59=1.7%（比例骤降）→ 触发合并 → 救回

**结构性矛盾**：PUNCT 空后，一人之下（左 2 汉字、需合并救）和我能（左 1 汉字、需不合并滤）
的命运被锁在了一起——两人的不空比例都极小且接近，任何 merge-ratio 都无法同时满足
"一方触发合并救人"+"另一方不触发合并杀人"。

**教训**：PUNCT 原来在发挥着**功能性作用**——它不是字，但它提供了**不对称信息**：`我能` 的左邻
有 26 个 PUNCT（占比 46%）→ 让不空比例够高（>25%）→ 不触发合并 → min 正确杀掉了它。
一旦把 PUNCT 从"不空"中移除，这种区别力就没了。

→ 方向虽然错误，但证明了**问题不在 PUNCT，而在"单侧近乎空"应该是一个独立的豁免条件**。

---

### 2.1.13 — 第二次尝试（矩阵方案），在独立率上撞墙

**尝试**：2×3 矩阵（左/右 × H/P/E），派生三个判据联动——Total<8 豁免、Indep_Ratio<0.5 直接滤、
Imbalance>0.8 合并。

**致命缺陷出在 Indep_Ratio**：希望用 `independent/count` 区分自由词和寄生词。但数据说：
- `世界`：independent=34/263=**13%**——因为它是"完美世界"的子串，大部分出现被覆盖。13% 不代表它附属于什么。——步骤 2 直接把它杀了 ❌
- `长生`：independent=47/124=**38%**——同理被覆盖。——杀了 ❌
- `我能`：预期 independent=30/86=35%（应该被步骤 2 杀），实际 30/59=**50.8%**——刚好跨过 50% 豁免。——漏过了 ❌
- `剑修`、`剑客`：Total=3<8 → 步骤 1 豁免 —— 放了 ❌

score 从 0.920 暴跌到 0.735。

**本质教训**：`independent` 测量的是"不被更长词覆盖的比例"——**这是一个关于"这个词多短"的指标**，
不是一个关于"这个词多自由"的指标。短词的被覆盖率自然高于长词，无论它是不是寄生词。
把 independent 当自由词判据用，等于用一个不相关测量去判定是非。

---

### 2.1.14 · 2.1.15 — 第三条失败路线：独立率后置保护

**尝试**：不改变主判据，在熵过滤**之后**加一层恢复：把 independent/count ≥ 阈值的被滤词复活。

**阈值 0.25 时**：1285 个被熵滤的词里，**1247 个（97%）独立率 ≥ 0.25**。
阈值再拉高（0.55、0.80、1.0），最高 score 仅 0.846，始终比基线 0.906 低 6 分以上。

**2.1.15 换了顺序（前置豁免 vs 后置恢复）**：输出逐字节相同。
因为留存集合的数学形式恒等 = {bind通过 AND (熵通过 OR 独立率≥阈值)}，顺序不影响结果。

**本质教训**：独立率这个信号在 BFS 短片段上没有区分力——短词的独立率普遍高，
因为"经常独立成段"恰恰是 BFS 片段的天性。高独立率包括了你要的字（首富）也包括你不要的模板（诸天之）。
**不对的信号再多也是噪声。**

---

### 2.1.16 — 位置固定度豁免（当前推荐版）

**核心思路**：既然 2.1.12 证明了"一侧近乎空应该独立豁免"，2.1.8 证明了"一侧全空→忽略整侧"，
那把这两个合起来——**某侧"没有真实汉字邻居"的占比超过阈值，说明这一侧的"空"不是依附结果、而是位置使然。**

```
"无汉字邻居" = 空 + 符号
某侧占比 = (该侧空次数 + 该侧符号次数) / 该词总次数
≥ POS_FIXED_THRESHOLD → 该侧"位置固定"
  → 复合熵判据中忽略该侧，只看另一侧
  → 两侧都固定 → 豁免(-1.0)
```

**为什么必须把"符号"也计入位置固定度？**
如果只计空（SEP），`什么鬼` 右邻 12 次空 / 23 = 52%，远不到 80%。只有计入 PUNCT×10 后，
(12+10)/23=96% 才触发豁免。**符号邻居在这个语境里和空一样——都是"没真正的人/词依附它"。**

#### 逐档扫描（pf=0.55→0.95, me=0.5, mr=0.25）每个档放出了什么/杀了什么：

| pf | 新增数 | 放过的最坏词 | 多杀的正词 | 评判 |
|---|---|---|---|---|
| 0.55 | 325 | **诸天之**(75%)、联盟之(55%)、无限之、火影开、星空之 — 这 5 个是"之X"模板寄生，句首比 55~75% | 无 | ❌ 寄生泄漏 |
| 0.60-0.75 | 85~324 | 诸天之(~75%) 持续存活到 0.75；联盟之 在 0.60 消亡（低于 60%） | 无 | ❌ 仍有泄漏 |
| **0.80** | **13** | **我能**(98%) — 用户接受复活。诸天之(75%)<80% 被正确格杀 | 从零(**修掉基线误留**) | ✅ **甜点** |
| 0.85 | 3 | 仅 我能(98%)，用户接受 | 从零 | ✅ 保守可行 |
| 0.90 | 3 | 同 0.85 | 从零 | ✅ 保守可行 |
| 0.95 | 2 | 仅 我能(98%)，但**人在斗罗(100%) 漏救** | 无 | ❌ 过严 |

> `从零`(cnt=10, ind=1, 熵=1.21) 是一个**意外收获**——基线(pf=0)里它靠合并路径把"空左 + 右'开始'"
> 凑出熵 1.21 被误留。pf=0.80 触发左侧豁免 → 只看右侧 → 右仅"开始"一组 → 低熵 → 正确滤除。
> 它在低 pf 档（0.55~0.75）因为"一侧固定豁免"仍被留，因为合并依然生效；0.80 档才是边界。

#### 为什么 0.80 比 0.85 多救 10 个真实词，却一个寄生都不多放？

这 10 个词是：完美世界、重燃、君临、妙手、九星、黑客、魔法师、仙朝、仙医、神诡世界。

它们的共同特征：都是 **cnt=5、ind≥2 的真实自由词/书名**，在基线被低熵误杀，位置固定度在 80%~85% 
区间（刚好高于 0.80 但低于 0.85）。而寄生模板（诸天之 75%）的位置固定度根本不到 80%—— 
**80% 这条线恰好把"书名/自由词"和"语法模板寄生"的位置特征分开**。

---

### 2.1.17 — 凝固度（cohesion）：GitHub 上找到的外来方法

> **来源交代**：2.1.16 之后，为确认"是否重复造轮子、有无可借鉴的成熟思路"，在 GitHub 调研了"新词发现（New Word Discovery）"领域的知名项目（`bubblewu/build-dict`、`zhanzecheng/Chinese_segment_augment`、`bojone/word-discovery`、SmoothNLP 等）。发现本项目的复合熵 = 该领域的 **左右信息熵 / Accessor Variety**（冯氏 2004），且成熟方案普遍用**双信号**：凝固度 + 自由度，而我们此前只用单一熵信号。**凝固度（cohesion/PMI）即由此引入**——它是外来的成熟方法，不是本项目原创。以下先讲清概念，再进实验。

**概念：凝固度衡量"词内部字与字的绑定强度"。**

BFS 生长只是枚举"字串重复出现"，但一个词之所以成词，除了"对外自由"（熵管的），还要求"对内结合紧密"——两半段若只是松散共现（如 `之` + `巅` 恰好相邻），那它更像词缀碎片而不是词。

```
PMI(左,右) = log2( count(整词)·N / (count(左半)·count(右半)) )
```

- count(整词) 大而 count(左)/count(右) 小 → 左右两半**几乎总是绑在一起出现** → PMI 高 → 越像一个词；
- 两半各自单独大量出现（`之` 到处都是）→ PMI 低甚至为负 → 松散搭配。

对多字词取**所有切分点的最小 PMI**（min-PMI，取"最弱结合处"）作为该词的凝固度。

**它和复合熵的关系**：正交的两把尺子——
- 复合熵 = **外部**信号：看词的"邻居"自由不自由（左右信息熵，判别它是不是焊死在某搭配旁）；
- 凝固度 = **内部**信号：看词"自己内部"结合得紧不紧（判别它是不是松散拼凑的碎片）。

**为什么需要它**：2.1.11 遗留的 4 个漏网（`之巅/之神/之子/我只`）熵值都 >1.2，纯熵信号永远滤不掉——它们是"外部自由、内部松散"的一类（`之` 是万能前缀，熵管不住）。这正是凝固度的猎物。

**实测结论**（真实数据，title 管线 me0.5+mr0.25，--cohesion 1.5~2.0）：

| 词 | 熵 | 凝固度 | 结果 |
|---|---|---|---|
| 之神 / 之子 | 2.56 / 1.58 | 1.36 / 1.34（低） | ✅ 被滤（熵抓不到的漏网） |
| 之巅 | 2.81 | 5.20（虚高） | ❌ 漏网（PMI 罕见字缺陷） |
| 我只 | 1.26 | 3.73 | ❌ 漏网（真实强搭配，熵才能管） |
| 什么鬼 / 一人之下 | — | 高 | ✅ 保留 |

- 金标准 score **0.906 → 0.946**，额外滤掉约 700 个碎片（之我/是我/的人/天之…）。
- 但命中**经典 PMI 罕见字缺陷**：`巅` 只出现 15 次，分母被低估 → 之巅 凝固度虚高到 5.20 漏网；`我只` 是真实强搭配，凝固度压不动（属熵信号职责，但熵=1.26 又抬不动）。
- 阈值 1.5~2.0 是甜点；3.0 起过度滤除真词（keep 33/37）。

**一句话总结**：双信号方向被证明正确、有真实增益，但原始 PMI 对罕见字失真——这正是后续 2.1.18 探索 AMI/NPMI 等量化的动因，也是"外来成熟方法"在本项目落地的第一站。

---

### 2.1.18 — 凝固度路线演进：AMI / NPMI / 罕见半段惩罚（不敌 min-PMI）

**触发**：2.1.17 暴露 PMI 罕见字缺陷（之巅虚高漏网）后，用户决定在 2.1.17 分支上继续演进凝固度，方向锁定 AMI（平均互信息）等修正度量。

**尝试**：实现三种度量替代 min-PMI —— AMI（对词长归一化的互信息）、NPMI（归一化互信息）、rare-k（罕见半段惩罚，直接修正"巅只出现 15 次"导致的虚高）。

**实测结论（金标准验证）**：
| 度量 | score | 结论 |
|---|---|---|
| min-PMI@1.5（2.1.17） | **0.946** | 基准 |
| AMI / NPMI / rare-k | 均 < 0.946 | **三种修正度量都不敌原始 min-PMI** |

**为什么修正反而更差**：AMI/NPMI 把"之巅"压下来了，但也把一批真词（如只以罕见字为尾的真实书名）一起压低了——罕见字修正的副作用是误杀真词。rare-k 加了半段惩罚也救不回平衡。**默认回退 pmi**（commit `745312f`）。

**教训**：外来成熟方法不是越"高级"越好——原始 min-PMI 在这份语料上反而是最均衡的。这是"外来方法落地"的第二站：第一站（2.1.17）证明方向对，第二站（2.1.18）证明**换度量不如换维度**——真正的提升来自和熵正交的位置固定度（2.1.16），而不是在同一条轴上换公式。

---

### 2.4.1 — SPE 超词位置熵：第4信号（有向依赖/包含关系结构维度，main 直分）

> 编号说明：2.4.1 是重命名后的新号（方向③ SPE）。此前的 2.1.19 即它。实现见当前分支 grow.py（候选元组 5→6 元素，加 `spe` 列）。

**动机（从用户"有向依赖/包含关系"的抽象直觉来）**：前面的三信号都在回答"词与**邻居/自身**的关系"，
但语料里还有一层被忽视的结构——**词与更长词的包含关系**：`首富` 从属于 `从首富`、`霸道` 从属于 `霸道总裁`。
短词是长词的一部分，蕴含天然的偏序秩序。这个纵向维度此前从未被利用。

**定义**：对候选词 w，找语料中所有包含它的更长候选词 Super(w)（count≥2），
把 w 在超词里的相对位置记为**前缀/中缀/后缀**三桶，按超词频次加权，求位置分布的熵：

```
spe ≈ 0   → 永远焊死在同一构词位（词缀碎片：诸天之 / 之魂）
spe 高    → 能在不同构词位自由拼装（真构词件：首富 / 庆余年）
spe < 0   → 无合格超词（完整独立词或死端碎片，无法判断）
```

**调参结论（两条路线，一条死一条活）**：

1. **词缀过滤（--spe-affix）：灾难，放弃。** 用"位置固定"去删词，删的全是位置焊死的词，但真词
   （大佬/网游/崛起/法师…）和碎片一样位置焊死——阈值 0.4 砍 1417 词，其中大量真标题词。**"位置固定"分不清真词与碎片。**
2. **结构救援（--spe-rescue）：可行，定为 sr=0.8。** 只认"位置高度多样"（spe≥0.8）这一可靠信号，
   去救被熵门误杀、但能在不同构词位自由拼装的真词。

**实测对比（title 语料，me0.5+mr0.25，四版本 + SPE 变体）**：

| 版本 | 产词数 | Δv211 | 救 000 层真词 | 清共识碎片 |
|---|---|---|---|---|
| v211 纯熵基线 | 5865 | +0 | 0 | 0 |
| v216 位置固定度 | 5877 | +12 | 0 | 0 |
| v217 凝固度 | 5156 | −709 | 0 | 0 |
| v219=2.4.1 SPE关 | 5865 | +0 | 0 | 0 |
| v219=2.4.1 SPE救 sr=0.8 | **5895** | **+30** | **12** | 0 |
| v219=2.4.1 SPE救 sr=1.0 | 5871 | +6 | 4 | 0 |

- **纯增量性验证**：SPE 关闭时产词与 v211 完全一致 → 第4信号不与旧三信号冲突。
- **sr=0.8 净 +30**：救回 12 个 000 层标注真词（首富/庆余年/康熙/谍战/铁血/舰娘/围棋/唐门/神诡/明月/捡属性/梦幻），代价搭进 18 个噪声（聊天/联盟之/罗之/路明…）。
- **标签污染**：12 个"救回真词"里 聊天/让他/工程 三个其实是噪声（000 层标签自身污染），干净真词 9 个。

**为什么救真词必带噪声（这个版本的"墙"）**：位置多样的碎片（聊天 spe=0.92、联盟之 spe=0.97）和位置多样的真词
（首富 spe=0.86、庆余年 spe=0.99）**长得一模一样**——位置这一维切不出干净解。这撞上和"独立率陷阱"同构的墙：
都在测"能不能拼/拼几个位置"，而不是"拼装时扮演什么角色"。

**定位**：SPE 是唯一能把手伸进 000 层（三版一致误杀层）的信号，但当前形态精度上限 9/30≈30%——定位为
**可信度辅助列/弱救援信号**，不作主判据。产物：`exp/v219_out/title_wordfreq_sr0.8.csv`（含 spe 列）。
后续改进方向见文末"六、抽象模型与后续改进方向"。

### 2.4.2 — 补集偏序 RSR：第5信号试探（方向③内部迭代，2.4.1 后继）

> 编号说明：2.4.x = 方向③（自主偏序路线）内部迭代，不碰凝固度（外来路线留到另一分支）。
> 实现见当前分支 grow.py（候选元组 6→7 元素，加 `rsr` 列）；对比脚本 `exp/cmp_242.py`。

**动机（把 6.2 的"剩余结构"洞察落进代码）**：SPE 只看了"w 在超词里站哪个槽位"，丢了"扣掉 w 后超词剩下什么"。
用户进一步指出补集 r 自身也处于偏序（从首富扣掉首富剩「从」，而「从」又从属于从军/从此/从今）。
据此定义**补集泛用度 U(r) = 把 r 当补集的不同超词数**，**RSR(w) = 各超词按频次加权的 U(补集) 均值**。
语义：补集泛用度高（首富：补集从/成 U 高）→ w 是主干（留）；补集专用实词（联盟之：补集王者 U 低）→ w 是附件（滤）。
枚举范围从「候选集内 len≥2」扩到「含单字 len≥1」，补集常为单字（从），与 SPE 同一遍超词遍历。

**调参结论（两条路线，皆未达预期——印证 6.1/6.2 的陷阱预警）**：

1. **RSR 救援（--rsr-rescue，与 --spe-rescue 取 AND）**：仅边际改善。spe≥0.8 救援集 30 词（12 标签 / 13 净干净 / 18 噪声），
   加 `rsr≥8` 后剩 24 词（10 标签 / 12 净干净 / 14 噪声）——清掉了聊天/联盟之/我自/是这样 4 个清晰碎片，
   但**丢了 2 个 000 标签**，且终极陷阱「真没/我终」因补集含超常见字（我/的）rsr>180，**任何阈值都滤不掉**。
   计划目标（噪声≤8 且 000 召回≥12）**不可同时达成**。
2. **RSR 词缀过滤（--rsr-affix）：灾难，放弃。** 与 spe_affix 同构的坑——位置焊死且补集实词≠碎片。
   rsr_affix=2 删 363 词，其中**误杀 32 个已知真标题词**（指南/有点/历史/天道/刘备/侦探/律师/星球/主宰/王座…），
   因这些真词补集恰是稀有字（天道之后缀罕见）→ rsr 低 → 被误判为附件。spare 了大佬/网游（其补集常见字→rsr 高），
   但代价是成片误杀 welded 真词。

**实测对比（title 语料，me0.5+mr0.25）**：

| 版本 | 产词数 | Δv211 | spe≥0.8 救援集 | 000标签 | 净干净 | 噪声(估) |
|---|---|---|---|---|---|---|
| v211 纯熵基线 | 5865 | +0 | — | 0 | 0 | — |
| v217 凝固度 | 5156 | −709 | — | 0 | 0 | — |
| 2.4.1 SPE救 sr=0.8 | 5865→5895 | +30 | 30 | 12 | 13 | 18 |
| **2.4.2 SPE+RSR sr=0.8,rsr≥8** | **5865→5889** | **+24** | **24** | **10** | **12** | **14** |

**为什么 RSR 没成第5信号（核心教训）**：「补集常见度」与「w 是不是真词」只是弱相关。
真词补集常是常见功能字（从/的→U 高），但噪声补集也含常见字（我/的真没→U 高，被误判为真）；
而 welded 真词（天道/历史）补集恰是稀有字→U 低→被误判为附件。指标实际测的是"补集是否常见字"，
与"w 是否真词"差了一层。这与 6.1 预警的「独立率陷阱」同构：**测错了变量**。

**定位**：RSR 实现正确、可计算，但作为**自动闸门不达标**——定位为 `rsr` **辅助列**（供人工/半自动复审排序加权），
不作判据。真正落地第5信号需先解决"补集常见字陷阱"（如功能字词典、或换一种不依赖补集字频的聚合）。
产物：`exp/v242_out/title_wordfreq_241.csv`（含 rsr 列）、`title_wordfreq_242.csv`。

---

## 三、调参精要（一张表看懂三个参数的"跷跷板"）

每个参数都是一个跷跷板——**一端是"放过寄生词"，另一端是"误杀真实词"。没有免费午餐。**

### 3.1 min-ent：决定"多僵才算僵"

| 调低(松) | 调高(严) |
|---|---|
| ✅ 保留更多真实低频词（邻据稀疏→天然低熵） | ✅ 寄生碎片全部格杀 |
| ❌ **寄生复活**：寄生词一侧邻居单一→熵≈0，阈值 <0.3 时对其完全无效 | ❌ **低频自由词误杀**：cnt=5、邻据不足 3 种的词，熵本就在 0.5~1.0 区间。提高阈值 = 连它们一起杀 |
| me=0.3: filt 仅 6/25（大量寄生逃跑） | me=0.7: 多杀 65~101 个真实词 |

**定位技巧**：跑一次没过滤的候选集，做熵值直方图。寄生集群在 [0.0,0.3]（BFS 片段→退化邻居→熵≈0），
自由词集群在 [1.0,1.5]。阈值放两峰之间的谷——本语料是 0.5。

### 3.2 ent-merge-ratio：决定"少侧多穷才该合并"

| 调低(松=少合并) | 调高(严=多合并) |
|---|---|
| ✅ 严格 min：一侧有依附、另一侧再自由也没用 | ✅ 合并路径救回"一侧近乎空"的真实书名（一侧信息不足，不应只用有信息侧做 min） |
| ❌ **漏救**：一侧几乎全空的真实词被 min 杀（苟在 13%、完美世界 18%） | ❌ **漏滤**：合并把双侧揉在一起 → 总熵比 min 高 → 边界寄生词逃脱（之巅、之魂在 mr=0.30 时被合并路径救） |

**理解合并的本质**：合并路径不是在"说"这个词不依附——它是说"一侧信息太少，不足以为判据"。
所以把两侧都算上报总熵。当总熵够高时，即使某侧确实依附，全视野也认为它够自由。
这是"宁可错杀少，不可错放多"的一种软化——在 merge-ratio 的合理区间内找出最优软化点。

### 3.3 pos-fixed：决定"多少位置占比才算假象"

| 调低(松=更多豁免) | 调高(严=更少豁免) |
|---|---|
| ✅ 多救回句首/句末真实词 | ✅ 严格：不放过任何语法模板 |
| ❌ **之X模板泄漏**：诸天之(75%)、无限之(65%) 等语法助词→句首比例在 55~75% → 满足 <0.80 的阈值 → 被当"位置固定"豁免 ✓ → 只看另一侧→右侧自由→高熵→保留 | ❌ **漏救**：完美世界(82%)、重燃(81%)、人在斗罗(100%) 的位置固定度在 80%~100% 区间，阈值 >80% 就把它们漏了，而 0.95 连人在斗罗也漏 |

> **核心教训**：pos-fixed 是一条"你有多少把握说这个词的低熵只是位置假象"的刻度尺。
> 阈值越低=把握越低=要求越松=放过去的越多。0.80 恰好是语法模板（55~75%）够不到、
> 书名/自由词（80%+）能够到的分界线。

---

## 四、失败实验的本质教训

三条路线（PUNCT 空化 / 矩阵方案 / 独立率保护）失败，根源各不同，但有一条共同规律：

**不要用一个本身不区分问题的信号去解决问题。**

| 路线 | 信号 | 为什么没区分力 |
|---|---|---|
| PUNCT 全局空化 | 全/空二元 | PUNCT 本是不对称信息源头——空掉它反而抹掉了唯一能分出"我能"和"一人之下"的区别 |
| 矩阵方案 | independent 比例 | independent = "不被更长词覆盖的比例" ≈ "这个词多短"。短的寄生模板（剑修=3 字）和短的合法词（世界=2 字）在这条轴上完全重叠 |
| 独立率保护 | independent 比例 | 97% 的被滤 BFS 片段本来就独立率≥0.25——因为短 ≠ 不独立。高独立率既是真实词的特征，也是短寄生词的特征 |

---

## 四·五、项目定位：原创 vs 外来（诚实账本）

**为什么要有这一节**：本项目在演进中引入了 GitHub 上的成熟方法（凝固度）。分清"哪些是自研原创、哪些是外来方法"，既是工程诚实，也是理解项目价值的钥匙。

### 外来方法（非本项目原创，来源已交代）

| 方法 | 领域出处 | 在本项目的位置 |
|---|---|---|
| **复合熵 / min(左熵,右熵)** | "新词发现"领域的**左右信息熵 / Accessor Variety**（冯氏 2004），Matrix67 2015 年博客系统化 | 2.1.4~2.1.16 全链路——**用户独立重新发明**（未参考外部即设计出等价判据），但事后 GitHub 调研确认是成熟方法 |
| **凝固度 / min-PMI** | `bubblewu/build-dict`、`zhanzecheng/Chinese_segment_augment`、`bojone/word-discovery`、SmoothNLP 等 | 2.1.17 引入为第二信号（明确标注外来） |
| **AMI / NPMI** | SmoothNLP 等 | 2.1.18 试验，均不敌 min-PMI，回退 |

### 自研原创（成熟方案没有对应物）

| 原创点 | 版本 | 解决的问题 | 成熟方案为何没有 |
|---|---|---|---|
| **独立出现次数判据** | v1 | 分布式嵌套盲区（霸道 被精准删除） | 标准实现只做最大重复，不做"独立成段"判定 |
| **邻居三态（汉字/PUNCT/空）+ PUNCT 折叠计数** | 2.1.5~2.1.10 | 标点提供不对称信息、但不作"有邻居"证据 | 标准实现普遍直接**丢弃**标点 |
| **合并熵模式**（少侧/多侧 <25% 合并） | 2.1.6 | 一侧近乎空的真词被 min 误杀 | 标准实现只会 min 或平均，没人做"信息不足就合并" |
| **"数据太少回答不了归属就不滤"豁免** | 2.1.10 | 斗破苍穹/诛仙/史记 等低频名著 | 标准实现按频次硬切 |
| **位置固定度豁免** | 2.1.16 | 句首/句末"位置假象"误杀真词 | 标准实现假定连续长文本，没人处理"一行一个标题"的 run 边界伪影 |
| **SPE 超词位置熵**（有向依赖/包含秩序，第4信号） | 2.4.1 | 三版一致误杀的 000 层真词（首富/庆余年/康熙） | 标准方案用"凝固度+自由度"双信号，不含"词在更长词里的构词位自由度"这一纵向维度 |
| **错题集分层方法论** | 验收 | "共识≠正确"的算法交叉审计 | 无 |

**一句话定位**：成熟方案用**双信号**（凝固度+自由度）解决"新词发现"通用问题；本项目在解决自己的数据形态问题（标题语料、BFS 片段）时**独立逼出了等价甚至更好的判据**，并补了成熟方案的两个盲区（run 边界位置假象、分布式嵌套）。

---

## 五、最终参数与效果

| 参数 | 值 |
|---|---|
| `--min-ent` | 0.5 |
| `--ent-merge-ratio` | 0.25 |
| `--pos-fixed` | 0.80 |
| `--bind` | 1.0（关闭） |

**效果（起点标题语料，7150 全量候选）：**

| 指标 | 数值 |
|---|---|
| 保留词数 | 5877 |
| 被滤词数 | 1283 (17.9%) |
| 金标准 keep | 36/37 |
| 金标准 filt | 20/25（+"我能"用户接受复活 = 21/26 实际） |
| score | 0.886 |

> ⚠️ **金标准降级说明**：本节及前文各版本的 62 词金标准 score，**只适合调参时看"变化方向"**——
> 它已被文末"三版本最终验收评估"证明不可信（高估凝固度、低估位置固定度、同构词双标）。
> 正式验收以生产指标为准：**硬碎片残留数、高频(count≥5)误伤数、top 高频词质抽查**。详见文末。

**新增 vs 基线(pf=0)的 13 词**：我能 / 什么鬼 / 人在斗罗 / 完美世界 / 重燃 / 君临 / 妙手 / 九星 / 黑客 / 魔法师 / 仙朝 / 仙医 / 神诡世界 — 全部是真实自由词或书名。

**减少 vs 基线的 1 词**：从零 — 基线误留的寄生碎片，被位置固定度豁免顺带修掉。

**已知局限**：① `首富`(右固定度 62% < 80%，不触发豁免)仍被滤——它在语料里的"句末"特征不够强，不是 pos-fixed 能救的范围；② `什么鬼` 虽已救回，但"右邻极度贫瘠+左侧自由"这类词未来可能还有新的误杀，需要逐词排查。

---

## 附录：对照表 — 关键版本的问题与推动的下一个版本

| 版本 | 发现的缺憾 | 缺憾的真数据表现 | 推动的改进 |
|---|---|---|---|
| v1 (独立判据) | `independent` 只挡"被更长词覆盖"的嵌套寄生，挡不住"焊死在固定搭配旁"的邻接寄生 | 之魂 永远跟"X之魂"、聊天 92% 紧邻"群" | → 2.1.4 复合熵（min 判据） |
| v2.1 (去耦) | 模型验证"内联=去耦"用自己比自己（循环论证） | 重跑两次布局不同（wordcloud 无随机种子） | → 固定 random_state，产物可复现 |
| 2.1.1 (空壳) | 内联 HTML 随语料膨胀（intro 管线/18万本会爆） | 8888 书名 HTML=322KB | → 数据/引擎解耦：外壳 + data.js |
| 2.1.3 (搜索栏) | 搜索功能让用户发现"邻接寄生"真噪音 | 之下 21/23 嵌"一人之下"、界的 依赖"世界的"、生从 集中"长生/重生" | → 2.1.4 复合熵（功能→认知→算法闭环） |
| 2.1.4 (纯min) | "全独立书名"和"焊死依附"在 min 里长得一样 | 吞噬星空: min(0,1.43)=0 → 误杀 | → 2.1.6 合并熵 |
| 2.1.6 (合并熵) | PUNCT 被算成"真实邻居" | 长生修仙: 右 PUNCT 让豁免判定失效 → 误杀 | → 2.1.7 PUNCT 豁免分离 |
| 2.1.7 | "一侧全无效数据"也被 min 压成 0 | 万族: 左全空=左熵 0 → 误杀 | → 2.1.8 全空→忽略整侧 |
| 2.1.8 | "几乎全空但不是全空"掉进 min 盲区 | 一人之下: 左仅 2 次汉字/21 次 → 不触发全空保护 → 误杀 | → 2.1.10 定稿规则 |
| 2.1.10 | "句末固定词"的右侧高度空化被误判为依附 | 什么鬼: 右 1/23 汉字=右min=0.44 → 误杀 | → 2.1.16 位置固定度豁免 |
| 2.1.16 | "句末固定度不够高"的真自由词仍无保护 | 首富: 右固定度 62%→不触发豁免 | → 未解决（需定向白名单） |
| 2.1.17 | 熵信号抓不到"外部自由、内部松散"的碎片 | 之神/之子 熵>1.2 永远滤不掉 | → 2.1.17 引入外来凝固度（PMI）作第二信号，与熵正交 |
| 2.1.18 | min-PMI 罕见字失真（之巅虚高漏网） | 之巅 凝固度 5.20（巅 仅 15 次） | → AMI/NPMI/rare-k 修正度量实测**均不敌 min-PMI**，回退 pmi |
| 2.4.1 (SPE) | 三版本在数学上动不了 000 层（三版一致误杀的真词） | 000 层 60 词里混着 27 个凝固度≥6 的疑似真词 | → 第4信号 SPE 超词位置熵（纵向包含秩序），sr=0.8 救回 12 个；但精度上限 9/30，暴露"剩余结构"才是下一刀 |

---

## 三版本最终验收评估（2026-08-13，生产视角）

> **前提衔接**：本评估对比的三个版本是——main 2.1.11（纯复合熵）、2.1.16 位置固定度（自研）、2.1.17 凝固度（外来方法，概念与来源见上节 2.1.17）。以下结果基于同一语料、同一基准跑出的真实数据。

> **结论先行**：此前的 62 词"金标准"**不可信**——它严重高估了凝固度路线、低估了位置固定度路线。改用贴合生产（统计网站标题取名高频词）的维度重新评估后，**位置固定度（自研）与凝固度（成熟 PMI）确实各有千秋，且高度互补**：位置固定度救真词、凝固度清碎片，一个管"别误杀"，一个管"别漏滤"。

### 1. 旧金标准为什么不可信（三个客观证据）

1. **KEEP 集缺真实词**：37 个"应留"词里没有 `完美世界/重燃/什么鬼/人在斗罗/庆余年/君临` 等真实书名/自由词 → 凝固度路线把它们全滤了，金标准一分不扣，只因滤对 `之神/之子` 就加分 → **凝固度被高估**。
2. **FILT 集含主观词**：25 个"应滤"词里有 `我能`，而位置固定度路线按用户意图救活了它 → 救回 5 个真词不加分、反因"我能"扣分 → **位置固定度被低估**。
3. **同构词双标**：`之主/之王`（词缀模式）在 KEEP 加分，`之神/之子`（同构词缀）在 FILT 加分——人为按频次划线，不是客观词性准则。

**用途边界**：62 词打点只适合调参时看"变化方向"，不能当验收标准。

### 2. 实测数据（同一语料：起点标题 7150 候选；同一基准 me0.5+mr0.25；v216 加 pf0.80、v217 加 coh1.5）

| 指标 | main 2.1.11 | pos-fixed 2.1.16 | cohesion 2.1.17 |
|---|---|---|---|
| 输出词数 | 5865 | 5877 | 5156 |
| 硬碎片残留 | 217 | 218 | **113** |
| 旧金标准 score | 0.9065 | 0.8865 | 0.9465 |
| 高频(count≥5)误伤 | 73 | **61** | 198* |

\* 凝固度 198 个"误伤"里多数是该滤的句法碎片（的我/之我/人的/子的），但真误杀也不少（见发现一）。

### 3. 三个关键发现

**发现一：位置固定度（v216）救真词能力最强。** 完美世界、重燃、什么鬼、人在斗罗、九星、仙朝、仙医、黑客、魔法师、妙手、神诡世界、君临 —— **这 12 个真实书名/自由词在 main 和凝固度里全被误杀，只有位置固定度保住了它们**。它把"出现在标题首尾、熵低但其实是位置假象"的真词救回来，是 main/凝固度都没有的能力。

**发现二：凝固度（v217）清碎片能力最强。** 硬碎片残留 113 vs 217，砍掉一半——`之神/之子/我只/联盟之` 这类熵抓不到的中等碎片被凝固度清掉，熵+位置固定度都留不住它们。

**发现三（最有价值）：两者在真实数据上完美正交。** v216 救的词（完美世界/重燃/什么鬼/人在斗罗…）在 v217 里全被误杀；v217 滤的词（之神/之子/我只…）在 v216 里全保留。**救真词的信号不清理碎片，清碎片的信号会误杀真词** —— 一个管召回、一个管精度，恰好互补，这是"双信号组合"最强的实证依据。

**共同盲区**：高频句法碎片（`我的/我在/我是/我有/这个/什么/成了/一个`）count 极高、凝固度也高，熵、位置、凝固度三个信号全部滤不掉，top 高频榜被污染。需**句法停用词表**这一新维度。

### 4. 生产结论（统计网站标题取名高频词）

- **三选一投产：选 v216 位置固定度**——误伤最少（61）、真词保留最多，最贴合"取名高频词"统计。
- **最优解不是三选一，是组合**：v216 位置固定度（救真词）+ v217 保守凝固度（清中等碎片）+ 句法停用词表（清"我的/这个"）。两个自研/外来信号已被证明正交，组合即精度与召回兼得。
- **对自研方法的定位**：位置固定度是从真实误杀数据（什么鬼/完美世界 被熵判死）里悟出的针对性设计，成熟 PMI 方案没有对应物——**不是"追赶成熟方法"，是补了成熟方法的盲区**。代价是碎片清理弱于凝固度。各有千秋，组合即最优。

### 4.5 错题集（三版本交集/差集分层标注，2026-08-13）

**做法**：比较三个版本的完整保留词集，按 v211/v216/v217 的"留/滤"位编码分 8 类，每词标注被哪个算法识别/排除，附加在原数据之上（`out_real/title_wordfreq_annotated.csv`，7150 词全量）。脚本 `exp/mistake_book.py` + `annotate_words.py`。

| 类别 | 层名 | 词数 | 含义 |
|---|---|---|---|
| **111** | 共识层 | 5155 | 三版都留，识别难度最低、优先级最低 |
| **110** | 凝固度排除 | 709 | v211+v216 留、v217 滤（多为低凝固度碎片：的我/之我/是我） |
| **101** | 位置排除 | 1 | 从零（v216 独门修掉的基线误留） |
| **010** | 位置独留 | **13** | 位置固定度独门救回的真词（价值最高：完美世界/重燃/什么鬼/人在斗罗/君临…） |
| **000** | 众矢之的 | 60 | count≥5 但三版全滤的候选 |

**两个最关键的发现**：

1. **010 层 = 位置固定度的独门贡献全集**：13 个词熵从 0 被救到高值、凝固度也高（8.1/6.1/5.9…），佐证"它们是真词、是被熵误杀的"——与"发现一"完全互证。
2. **000 层暴露"三版共识 ≠ 正确"**：60 个被三版一致滤掉的高频候选里，混着 **27 个凝固度≥6 的疑似真词**（庆余年/康熙/首富/明月/工程/刺客/围棋/迪迦/首辅…）和真碎片（联盟之/诸天之/罗之/之开）。**三个算法一致同意滤掉的东西里藏着一批真书名**——这是下一步最该复核的"共同盲区反向样本"，也是"金标准不可信"的最硬实证（庆余年连"应留"名单都没进）。

### 5. 下一步优化方向（已立项，待实施）

**目标：双信号最终版**——把两个已被实测证明正交的信号合入一个版本，让精度与召回兼得。

1. **位置固定度 + 凝固度双信号**：在 v216 位置固定度豁免（救真词）基础上叠加 v217 凝固度闸门（清中等碎片）。实测 v216 救的词 v217 全杀、v217 滤的词 v216 全留，正交互补，组合是唯一能"既保真词又清碎片"的路径。
2. **真词保护（组合的关键细节）**：凝固度阈值必须**保守**（v217 的 1.5 会把 完美世界/重燃/什么鬼/人在斗罗 等真词误杀），且对位置固定度救回的词**豁免凝固度闸门**，避免 v217 的误杀问题在组合里复发。
3. **句法停用词表（三版本共同盲区）**：`我的/我在/我是/我有/这个/什么/成了/一个` 这类 count 极高、凝固度也高的句法碎片，熵/位置/凝固度全部滤不掉——需要第四维度"句法停用词表"从 top 高频榜剔除。
4. **复核错题集 000 层（共识≠正确）**：把 27 个疑似真词（庆余年/康熙/首富/明月…）拉出来逐词复核，决定是否以**定向白名单**形式救回——这是"生产结论"落地前必须回答的问题。
5. **评估口径更新**：弃用旧的 62 词金标准（已被证明高估凝固度、低估位置固定度），改用生产指标验收——**硬碎片残留数、高频(count≥5)误伤数、top 高频词质抽查、错题集分层占比** 四者并重。

---

## 六、抽象模型与后续改进方向（2026-08-13，思维方向转变）

> 这一节记录的不是某个版本的具体调参，而是一次**思维层级的转变**：从"平行判据投票"
> 转向"按结构分层、可线性推导"。它由用户的抽象思考触发，并据此推导出后续改进方向。

### 6.1 抽象模型：四个信号各在处理什么

把四个判据放在同一张图上，它们回答的是词库的**三个方向**的问题：

| 信号 | 维度 | 问的问题 | 本质 |
|---|---|---|---|
| **复合熵** | 横向·外部 | 词身边的位置上能不能换人 | 邻居自由度（左右信息熵） |
| **位置固定度豁免** | 横向·边界特例 | 词是不是焊死在边界位 | 邻居结构的特例补丁（"空"也是合法邻居态） |
| **凝固度** | 纵向·内部 | 词内部两半是绑定还是偶遇 | 字间结合紧密度（min-PMI） |
| **SPE 超词位置熵** | 纵向·外部（跨词） | 词在更长超词里站哪个槽位 | 包含秩序的一阶投影（偏序） |

关键澄清（本模型纠正的两处认知）：

1. **凝固度不看"跨位置"**——它完全不看位置，只看词的**内部**：把一个词从中间掰成两截，
   问这两截是总绑在一起出现、还是各自经常散开。它和复合熵正好是**里外一对**：复合熵看外部自由度
   （能不能自由交朋友），凝固度看内部紧密度（里面的字是不是铁哥们分不开）。
2. **SPE 是纵向维度的第一层**——包含关系确实是偏序结构，词库天然有层级。但 SPE 目前只投影了
   秩序里**一格**信息：位置（前缀/中缀/后缀）。

### 6.2 核心洞察：位置只是"包含秩序"的一阶投影，信息在"剩余结构"

**SPE 只看了秩序的一半。** 它记录"w 站在前/中/后哪个槽位"，却丢掉了**"扣掉 w 之后，超词剩下的是什么"**。
后者才携带真正的构词角色。实测证据（sr=0.8 救回的 30 词按"剩余部分"分类）：

```
【真词】首富   超词: 首富从 / 成首富     剩余: 『从』『成』  = 虚词 → w 是主干
【真词】庆余年 超词: 从庆余年 / 从庆余年开始  剩余: 『从』『开始』 = 虚词 → w 是主干
【噪 声】联盟之 超词: 全是实词剩余 → 100%成词 → w 是附件
```

**真词在超词里几乎都是主干**（剩余部分是"从/成/开始"这类句法虚词，w 是被修饰的核心）；
**碎片是附件**（剩余部分是实词，w 只是粘上去的尾巴）。**剩余结构的"虚词/实词比例"能把 首富/庆余年
和 联盟之 干净分开**——位置熵做不到的，剩余结构做到了。

**这解释了 SPE 撞墙的根因**：位置多样的碎片（聊天 spe=0.92）和位置多样的真词（首富 spe=0.86）
长得一模一样——因为两者都在测"能不能拼/拼几个位置"，而真正分清真词与碎片的变量是
**"拼装时扮演什么角色"（主角 or 配角）**。这和第 4 维的独立率陷阱同构：**测错了变量**。

### 6.3 递归视角：偏序的传递性（用户提出的下一层）

用户进一步指出：`首富` 从属于 `从首富`，而 `从首富` 扣掉 `首富` 剩下的 `从`，**本身又从属于
`从军/从此/从今`……** 也就是说：

- **偏序是传递的**：w 的剩余部分 r，自己也处于偏序中，顺着这条链可以一直往下走；
- 每个节点的"剩余部分"，在更高一层又是一个新的 w；
- **这整条链就是"线性"**——不是平行投票，是沿着包含链逐层传导的结构。

**工程上的关键契合（用户指出的，已验证成立）**：算凝固度的 `freq` 表里，每一个子串**同时是**
"凝固度的一块拼图"和"偏序结构的一个节点"——超词在表里，超词扣掉 w 后的剩余部分也在表里。
**凝固度与偏序结构共用同一份统计，零额外成本**，这正是"契合度非常好"的数学根源。

### 6.4 双向交叉验证（内部 × 外部 的联合判据）

| | 内部（凝固度） | 外部（剩余偏序） |
|---|---|---|
| 问什么 | w 的字绑得紧不紧 | w 在超词里是主干还是附件 |
| 真词信号 | 紧 | 剩余是虚词 → 主干 |
| 碎片信号 | 松 | 剩余是实词 → 附件 |

**内紧 ∧ 外主 → 双重真词信号；内松 ∧ 外附件 → 双重碎片。** 两个正交维度对同一结论交叉背书，
这是下一版判据的 2D 决策面。

### 6.5 后续改进方向（展望，未立项）

按优先级列出（全部可复用 freq 表，零额外语料成本）：

1. **RSR（Remainder-Side Ratio，剩余实词比）**：对每个词算"超词扣掉 w 后剩余部分为实词的比例"——
   比例高 → w 是附件（联盟之/之魂）→ 滤；比例低 → w 是主干（首富/庆余年）→ 留。直接补上位置熵盲区。
2. **剩余偏序深度**：w 的剩余部分 r，r 从属于多少词——r 越"泛用"越虚（从/之/开始），越独立则 w 越像附件。
3. **递归包含链**：沿偏序链每层的剩余结构作为叠加信号，实现"沿着包含链逐层传导的线性推导"。
4. **凝固度 + 偏序联合判据**：内紧 ∧ 外主 才是真词，作为 2D 决策面（取代当前 1D 阈值投票）。
5. **诚实边界**：RSR 不是银弹——sr=0.8 的 30 词里 14 个"混合类"（捡属性/铁血）RSR 单独切不干净；
   聊天这类标签污染它也无能为力。它应作为叠加在 SPE 之上的第 5 个弱信号，而不是替代品。

### 6.6 一句话总结

**凝固度看内部，复合熵看横向外部，位置固定度是横向的特例补丁，SPE 看纵向结构——而纵向结构里
最有信息量的"剩余结构"（主角 vs 配角）还没用上。** 那个方向做出来，才是真正把词库从
"平行判据投票"变成"有层级的线性推导"。

### 6.7 实践检验（2026-08-13，2.4.2 把 RSR 落进代码后）

> 6.5 的 RSR 已从"展望"变为"已试"——结果印证了 6.1/6.2 的陷阱预警，故补此节。

- **RSR 救援（AND 门）只边际有效**：spe≥0.8 救援集 30 词（18 噪声）→ 加 `rsr≥8` 剩 24 词（14 噪声），
  清掉聊天/联盟之等清晰碎片，但丢了 2 个 000 标签；终极噪声「真没/我终」因补集含超常见字（我/的）rsr>180，**不可滤**。
- **RSR 词缀过滤是灾难**：rsr_affix=2 误杀 32 个已知真标题词（指南/历史/天道/刘备…），与 spe_affix 同构坑。
- **根因**：RSR 实际测的是"补集是否常见字"，与"w 是否真词"只弱相关。真词补集常是功能字（从/的→U 高），
  噪声补集也含常见字（我/的→U 高），而 welded 真词（天道）补集恰稀有→U 低→误判附件。**又是"独立率陷阱"式测错变量。**
- **结论**：RSR 实现正确、可作 `rsr` 辅助列供复审排序，但**不作自动闸门**。要落地真第5信号，须先解决"补集常见字陷阱"
  （如功能字词典、或换不依赖补集字频的聚合方式）。详见上文 2.4.2 小节。

