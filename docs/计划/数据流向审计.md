# 数据流向审计（脚本接口 + 读写路径）

> 目的：确认沙箱内脚本的**生成文件/中间文件全部落在本文件夹**，不外溢。
> 审计日期：2026-08-13

## 结论

**所有写操作均落在沙箱内**。唯一外溢风险（tempfile 写到系统 %TEMP%）已修复，
临时目录统一改到沙箱内 `out_tmp/`（被 .gitignore 的 `out*/` 覆盖）。

## 逐项审计表

| 脚本 | 写操作 | 落点 | 沙箱内？ |
|---|---|---|---|
| `grow3/cli.py` | 词表 CSV / 词云 PNG/data.js/html/json / 审计 JSON | `--out` 指定目录（默认沙箱内 `out/`）；audit 自动建父目录 | ✅ |
| `grow3/output.py` | `write_word_csv(path,…)` | 由 cli 传入 out_dir | ✅ |
| `grow3/probe.py` | `audit.dump(path)` | cli 传入 | ✅ |
| `interactive_cloud.py` / `grow3/cloud.py` | 词云 html/js/json | out_dir | ✅ |
| `regress.py` | 中间词表 → `tempfile.TemporaryDirectory(dir=out_tmp)`；报告 → `exp/regress_report.txt` | 沙箱内 | ✅ |
| `exp/find_best_combo.py` | 中间词表 → `tempfile.TemporaryDirectory(dir=out_tmp)` | 沙箱内 | ✅ |
| `exp/compare_golden.py` | 报告 → `exp/golden_diff_report.txt` | 沙箱内 | ✅ |
| `exp/eval_versions/*.py` | mistake_book.csv/md、eval_result.json | `exp/eval_versions/` 内 | ✅ |
| `exp/run.py` | 各配置产物 | `exp/<name>/` | ✅ |
| `exp/tune_params.py` | 调参产物 | `exp/tune/` | ✅ |
| `exp/tune_combo.py` | 内存评估（一次 scan 多配置），无落盘 | — | ✅ |
| `tune_bind.py` | bind 明细 txt | `--out` 指定 | ✅ |
| `exp/cmp.py` / `verify.py` / `exp/probe_words.py` | 仅 stdout / 只读 corpus.csv | — | ✅ |
| Python 运行时 | `__pycache__/`（.pyc） | 各模块同目录 | ✅ |

## 修复记录

- `regress.py`、`exp/find_best_combo.py` 原用 `tempfile.TemporaryDirectory()`（系统 %TEMP%）
  → 改为 `TemporaryDirectory(dir=<沙箱>/out_tmp)`，中间词表不再外溢。
- 沙箱根 `.gitignore` 已含 `out*/`（覆盖 `out_tmp`、`out_real`、`out_demo` 等）。

## 运行约定

- 所有命令在**沙箱根**执行（`run.bat` 已 `cd /d %~dp0`）。
- 语料 `corpus.csv`、配置 `config.json`、评估集 `exp/eval_versions/` 均在沙箱内。
- 产物目录建议：词表/词云 → `out_demo/`；调参数据 → `调参产物/`；临时 → `out_tmp/`。
