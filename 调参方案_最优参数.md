# 生长词库 3.0 最优参数测试方案（详尽可操作版）

> 测试沙箱：`SANDBOX_ROOT`（独立副本，main 分支）
> 原则：**所有测试产物只写本目录，不外溢**；不改原 `history\` 项目。
> 状态：沙箱已验证——`regress.py` ALL PASS、基线组合 5149 词可跑、评估集 `exp/eval_versions/mistake_book.csv` 在副本内。

---

## 〇、目标

在 grow3 面板参数空间中找出**综合最优**配置（多个评估口径下的平衡点），并给出：
1. 每个参数的**单参数敏感性曲线**（控制变量）；
2. **极端参数**下的行为边界（崩溃/退化点）；
3. **梯度式**联合调参结果（深度学习式：损失函数 + 数值梯度 + 坐标下降）；
4. 最终推荐配置 + 与现有 5149（最干净）/ 5232（+SPE）的对比。

---

## 一、评估体系（先定义"最优"，再谈搜索）

### 1.1 评估集（已内嵌/可加载）

| 集合 | 来源 | 内容 | 期望 |
|---|---|---|---|
| `KEEP` | tune_params 金标准 | 37 个应保留词（吞噬星空/一人之下/世界/修仙…） | 全部保留 |
| `FILT` | tune_params 金标准 | 25 个应滤除词（我只/联盟之/之巅…） | 全部滤除 |
| `T000` | 错题集 000 层 | 15 个干净真词（庆余年/康熙/首富/围棋/谍战…） | 尽量召回 |
| `FRAG` | 错题集/实测 | 18 个碎片（我只/聊天/联盟之/罗之/我的/这个…） | 尽量滤除 |
| `RAW` | scan 候选 | 7150 个候选词（带 count） | 基数 |

### 1.2 指标

```
keep_rate  = |kept ∩ KEEP| / |KEEP|        （应保留的保留率）
filt_rate  = |kept ∩ FILT| 的补 / |FILT|   （应滤除的滤除率）
recall_000 = |kept ∩ T000| / |T000|        （000 真词召回）
frag_rate  = |kept ∩ FRAG| / |FRAG|        （碎片残留率，越低越好）
collateral = |(RAW−kept) 中 count≥5 且 ∉FILT∪FRAG|   （疑似误伤数，越低越好）
```

### 1.3 综合损失函数（深度学习思路的核心）

定义 **score**（越大越好）与 **loss L = 1 − score**：

```
score = w1·keep_rate + w2·filt_rate + w3·recall_000 − w4·frag_rate − w5·collateral_norm
collateral_norm = collateral / |{RAW 中 count≥5 的词}|
```

**三套权重**（对应不同产品偏好，最终一起报告）：

| 方案 | w1 keep | w2 filt | w3 recall_000 | w4 frag | w5 collateral |
|---|---|---|---|---|---|
| A 保守（保真） | 0.35 | 0.35 | 0.10 | 0.10 | 0.10 |
| B 均衡 | 0.25 | 0.25 | 0.25 | 0.15 | 0.10 |
| C 召回（救真词） | 0.15 | 0.15 | 0.45 | 0.15 | 0.10 |

> 梯度下降对 loss 求极小值；每套权重各跑一轮，避免"权重拍脑袋定死"。

---

## 二、参数空间（含极端范围）

### 2.1 连续参数（可求数值梯度）

| 参数 | 面板键 | 当前推荐 | **扫描/极端范围** | 语义边界 |
|---|---|---|---|---|
| 复合熵阈值 | min_ent | 0.5 | **0.0 ~ 2.0**（档 0.05） | 0=关；>1.5 基本全滤 |
| 合并触发比 | ent_merge_ratio | 0.25 | **0.0 ~ 0.9**（档 0.05） | 0=关（no_merge 等价） |
| 凝固度阈值 | min_cohesion | 1.5 | **0.0 ~ 10.0**（档 0.1/0.5） | 0=关；>5 高凝固词才留 |
| 词本身偏序阈值 | min_indep | 0.05 | **0.0 ~ 1.0**（档 0.01/0.05） | 0=关；1.0=只留完全不包裹 |
| SPE 救援阈值 | spe_rescue | 0.0 | **0.0 ~ 2.0**（档 0.05） | 0=关；>1.5 只救焊死位多样词 |
| RSR 救援阈值 | rsr_rescue | 0.0 | **0.0 ~ 50**（档 1/5） | 0=关；与 spe 取 AND |
| 前后集中度门 | bind_thresh | 1.0 | **0.5 ~ 1.0**（档 0.05） | <1.0 才激活 |

### 2.2 离散/枚举参数

| 参数 | 面板键 | 取值 | 处理方式 |
|---|---|---|---|
| RSR 聚合 | rsr_mode | mean / max | 嵌套枚举 |
| 超词门槛 | min_super_cnt | 1 / 2 / 4 / 8 | 嵌套枚举 |
| 标点感知熵 | no_punct_ent | false / true | 嵌套枚举 |
| 合并模式 | no_merge | false / true | 嵌套枚举 |
| 凝固度最大长 | cohesion_max_len | 4 / 6 / 8 / 12 | 嵌套枚举 |

---

## 三、方法学（四层递进）

### 3.1 控制变量法（单参数敏感性）

- **基准点** θ₀ = 当前推荐（me0.5, mr0.25, coh1.5, indep0.05, spe0, rsr0, bind1.0）。
- **规则**：每次只动一个参数，其余锁死在 θ₀；该参数按 2.1 档位全扫。
- **输出**：每参数一张表（参数值 → score/keep/filt/000/碎片/误伤），定位：
  - 单调区间、平台期、拐点；
  - 谁对 score 影响最大（**敏感性排序** = 后续梯度步长的依据）。
- 实现：`调参产物/sens_single.py`（一次 scan_once 复用，秒级全扫）。

### 3.2 极端参数边界测试

- 对每个参数取**极值**（如 min_ent=2.0、coh=10、indep=1.0、spe=2.0、mr=0.9、bind=0.5），
  其余锁 θ₀，单独观察：
  - 词数坍缩到多少（全滤阈值）；
  - 是否出现异常（负分/空表/崩溃）；
  - 极端值下哪类词最先牺牲（用 diff 看删词构成）。
- 目的：圈定**合法参数域**（后续梯度搜索的硬边界），并找出"危险区"。

### 3.3 梯度式调参（深度学习思路）

**思想**：把 θ 当可微参数，loss=L(θ)，用**数值梯度**估计 ∂L/∂θᵢ 找下降方向，
坐标下降 + 随机重启，逼近局部最优。

```
1) 初始化：θ ← θ₀（或随机搜索的 Top 点）
2) 数值梯度（每轮每参数）：
     εᵢ = 0.05·|θᵢ| + 0.01          （按量级自适应步长）
     gᵢ ≈ [L(θᵢ+εᵢ) − L(θᵢ−εᵢ)] / (2εᵢ)
3) 更新（坐标下降，一次动一维，优先动 |g| 最大的）：
     θᵢ ← clamp(θᵢ − η·sign(gᵢ)·stepᵢ, loᵢ, hiᵢ)
     stepᵢ = 0.05·|θᵢ|（阈值类）或 0.01（比率类）
4) 收敛判据：一轮内 score 提升 < 0.001 或达到 15 轮上限
5) 随机重启 ×5（θ 随机撒点 20 个 → 各跑梯度 → 取全局最优）
```

- 离散参数：在每轮坐标下降内**枚举嵌套**（rsr_mode×msc×no_punct_ent×no_merge 共 2·4·2·2=32 组合，
  对每个组合跑连续参数梯度，取最优）。
- 实现：`调参产物/tune_grad.py`，梯度每点一次 `scan_once` + 信号 + gate（秒级），
  一轮 ~7 参数 × 2 次评估 ≈ 14 次扫描 ≈ 十几秒，15 轮 ≈ 3 分钟 / 套权重。

### 3.4 全局随机搜索（防局部最优）

- 在合法参数域内**均匀随机采样** 50~100 个 θ（阈值在 log 或线性空间），
  三套权重各评一遍，取每套 top5。
- 与梯度结果交叉：随机 top 作为梯度重启种子。
- 实现：`调参产物/random_search.py`。

---

## 四、实施步骤（Phase 0–5）

| Phase | 内容 | 产出 | 产物文件 |
|---|---|---|---|
| 0 | 环境确认（已做）| 沙箱回归全绿、基线 score 记录 | `调参产物/phase0_基线.md` |
| 1 | 单参数敏感性（3.1）| 7 张敏感性表 + 排序 | `调参产物/sens_*.csv` |
| 2 | 极端参数边界（3.2）| 合法域 + 危险区 | `调参产物/extreme_*.csv` |
| 3 | 梯度式调参（3.3，三套权重）| 每套权重最优 θ + 轨迹 | `调参产物/grad_*.json` |
| 4 | 随机搜索 + 重启（3.4）| 全局 top 配置 | `调参产物/random_*.json` |
| 5 | 验证与交付 | 最优配置 + 回归验证 + 报告 | `调参产物/最优配置.json` + `最优参数报告.md` |

### 4.1 评估引擎（一次扫描多配置）

新建 `调参产物/tune_engine.py`，核心：
```python
def evaluate(cfg_dict) -> dict:   # 一次 scan_once，按需算信号，gate，返回全部指标
```
复用 grow3 的 scan/signals/gates，不重复扫描；供 Phase 1–4 全部调用。

### 4.2 最终验证（防过拟合）

- 用 `regress.py` 跑 6 个 golden 组合：**确认调参不破坏既有回归基线**（红线）。
- 用展示组合（最优 θ + no_cloud=false）跑一次真实产物，人工目检词云 top 词。
- 与 5149 / 5232 对比：给出"相对提升"表（每指标 delta）。
- **诚实记录**：若最优 θ 与 5149 几乎重合（说明 2.3.3 定档已是全局优），如实说明，
  不硬凑"新最优"。

---

## 五、产物与纪律

- **全部写入沙箱内**：脚本 `调参产物/*.py`，数据 `调参产物/*.csv/json`，报告 `调参产物/*.md`。
- 沙箱 `.git` 用于阶段性 commit（每 Phase 一次），**不 push、不与原项目混用**。
- 沙箱的 `out_real/`、`调参产物/` 均在 `.gitignore`（out*/ 已覆盖），避免产物进沙箱 git。
- 触碰语料/书名产物仍在本地，不外传。
- 每步实测数据 + 结论先入 `调参产物/运行日志.md`，最后汇总进报告。

---

## 六、预期输出

1. `最优参数报告.md`：每参数敏感性、合法域、危险区、梯度轨迹、随机 top、
   三套权重下的最优 θ、最终推荐（含理由）、相对 5149/5232 的提升表；
2. `最优配置.json`：可直接 `--config` 使用的最优面板配置；
3. 验证记录：regress ALL PASS + 展示样本产物。
