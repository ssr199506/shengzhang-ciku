# 调参运行日志

> 随手册逐步填写；本文件是 2026-08-13 完整执行的真实记录（n=100 随机搜索）。
> 所有产物在 `调参产物/` 与 `out_tmp/`，不外溢。

## Phase 0 基线
- 命令: `python 调参产物/tune_engine.py`
- 输出: `5149 keep36/37 filt 1/25 000 0/15 frag 4/18 coll159 | A=0.645 B=0.440 C=0.247`
- 结论: 引擎正常。collateral 已修正口径（旧 174 → 修正后 159，排除了被重复计罚的金标准词）。

## Phase 1 敏感性
- 粗跑排序（sens_A）: `coh 0.352 > indep 0.259 > ent 0.181 > spe 0.159 > bind 0.134 > rsr 0.096 > mr 0.041 > no_punct 0.026 > no_merge 0.023 > cml 0.002 > rsr_mode 0 > msc 0`
- 精跑 `min_cohesion [0,4]`: 峰 **1.5**（1.0→1.5 涨，→2.0 回落）
- 精跑 `min_indep [0,0.4]`: 0.0 时碎片暴涨 11/18（A 反降到 0.592），峰 **0.05**（当前档）；0.05 以上 keep 递减、coll 暴涨 → indep 不能为零、当前档最优
- 结论: coh/indep/ent 是主旋钮；当前定档 me0.5+coh1.5+indep0.05 经精跑确认是峰；spe 最优=0（关）

## Phase 2 极端边界
- 危险区（extreme_report.csv）:
  - `min_cohesion=10` ⚠️ 坍缩 6%（keep 3/37）→ clamp ≤4
  - `min_ent=2.0` 坍缩 44%（keep 27/37）→ clamp ≤1.2
  - `min_indep=1.0` 坍缩 66%（keep 12/37）→ clamp ≤0.4
  - `bind_thresh=0.5` 伤真词（keep 24/37）→ clamp ≥0.7（默认 1.0 即关，满足）
  - `ent_merge_ratio=0.9` 膨胀 120%（filt 9/25）→ clamp ≤0.9
  - `spe_rescue=2.0` / `rsr_rescue=50.0` 删 0 词 → 不干活，默认关

## Phase 3 差分坐标下降（--weight all）
- A: 起点即收敛 score=0.6449（θ₀ 即 A 口径局优）
- B: 0.4405 → 0.6206，路径 `mr 0.25→0.85, coh 1.5→3.5`
- C: 0.2472 → 0.6506，路径 `mr 0.25→0.85, coh 1.5→2.0`
- 三套最终 θ 统一 `spe=0 rsr=0 indep=0.05`
- 产物: `grad_A.json` `grad_B.json` `grad_C.json`

## Phase 4 随机搜索（n=100）+ 重启
- A top1 = 0.5340 < θ₀ 0.6449 → θ₀ 在 A 口径全局最优确证
- B/C top1（0.4349 / 0.4139）远低于差分收敛（0.6206 / 0.6506）→ 梯度搜索优于随机离散
- 重启验证: A 从随机 top1 重启只到 **0.6131**；C 重启到 **0.6350**；均低于 θ₀ 起点 → 全局最优坐实；重启终点带 spe/rsr>0 反而更低
- 产物: `random_all.csv` `random_top.json` `start_A.json` `start_C.json`

## Phase 5 验证与交付
- 回归: `python regress.py` → **ALL PASS ✅**（golden 6/6, sha 6/6, scan_verify PASS）
- 真实产物: `python -m grow3.cli ... --out out_real` → 5149 词；top 词目检全真词（开始/重生/诸天/修仙/穿越/斗罗/火影…），碎片漏入 = 0；HTML 外壳 8KB + data.js
- 最终报告: `调参产物/最优参数报告.md`
- 统一口径结论:
  - 5149（默认）A=0.6449 = A 口径全局最优，随机重启翻不过
  - B 最优(mr0.85 coh3.5) A=0.6535（略高，但误伤 159→431、少保 5 真词）→ 分数高≠更实用
  - C 最优(mr0.85 coh2.0) A=0.6531，救回全部 15 真词、误伤仅 197 → 若重召回可备选
  - spe/rsr 救援：所有最优解关到 0，不启用

## 最终推荐
- **默认交付 = 5149 配置**（me0.5 mr0.25 coh1.5 indep0.05 spe0 rsr0），A 口径全局最优、最干净
- 重召回备选 = C 配置（mr0.85 coh2.0）
- 不启用 spe_rescue / rsr_rescue
